# Configuración de la API
# Esta URL se usa para el proxy de Vite en desarrollo
# En desarrollo, todas las peticiones a /api/* se redirigen a esta URL
# Para usar la API en Render (recomendado):
VITE_API_URL=https://pizzahub-api.onrender.com

# Para usar la API corriendo localmente:
# VITE_API_URL=http://localhost:5000
